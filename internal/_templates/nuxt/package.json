{
  "name": "nuxt-challenge",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "concurrently 'nuxt dev' 'vitest'",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test": "vitest --run"
  },
  "devDependencies": {
    "@types/node": "^18",
    "concurrently": "^8.0.1",
    "nuxt": "^3.4.2",
    "nuxt-vitest": "^0.6.10",
    "vitest": "^0.27.0"
  },
  "dependencies": {
    "@learnvue/challenger": "0.0.1"
  }
}
